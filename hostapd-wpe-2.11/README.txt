Despues de generar el paquete hay que entrar en live y ejecutar el fichero bootstrap y make , make install
puede fallar , hay que intentarlo hasta conseguir generar el fichero server.pem y luego copiar
/etc/hostap-wpe , dentro del empaquetado/modulo para tener los certicifados ssl.
